import cookies from "./second.js";
import $ from 'jquery';

console.log("JS is automatically compiled and sent to the page on save.");
console.log("Names is " + cookies);

function sum (x, y) {
  return x + y;
};
